{
  "_comment": "TypeScript Configuration",
  "_description": "TypeScript compiler configuration and type checking settings.",
  "_purpose": "Defines TypeScript compilation rules and module resolution.",

  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "src/*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}